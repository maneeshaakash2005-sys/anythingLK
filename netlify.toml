[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Pin Node version so the build environment matches local dev.
  # Vercel/Netlify default Node versions change over time and can break builds.
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
