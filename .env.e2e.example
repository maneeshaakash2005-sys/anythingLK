# Copy to .env.e2e.local (gitignored) or export before running authenticated E2E tests.
# Requires a Supabase project with VITE_* vars set for the build used by `npm run test:e2e`.

E2E_EMAIL=
E2E_PASSWORD=

# Optional overrides
# E2E_PORT=4173
# E2E_BASE_URL=http://127.0.0.1:4173
